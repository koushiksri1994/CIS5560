{"cells":[{"cell_type":"code","source":["# Import Spark SQL and Spark ML libraries\nfrom pyspark.sql.types import *\nfrom pyspark.sql.functions import *\n\nfrom pyspark.ml import Pipeline\nfrom pyspark.ml.regression import GBTRegressor\nfrom pyspark.ml.regression import LinearRegression\nfrom pyspark.ml.feature import VectorAssembler\nfrom pyspark.ml.tuning import ParamGridBuilder, TrainValidationSplit\nfrom pyspark.ml.evaluation import BinaryClassificationEvaluator, RegressionEvaluator\nfrom pyspark.ml.tuning import CrossValidator, ParamGridBuilder\nfrom pyspark.ml.regression import DecisionTreeRegressor"],"metadata":{},"outputs":[],"execution_count":1},{"cell_type":"code","source":["%fs ls /FileStore/tables/Book3a.csv"],"metadata":{},"outputs":[],"execution_count":2},{"cell_type":"code","source":["jobs = spark.sql(\"SELECT cast(Job_ID as double),cast(Posting_Type as int),cast(Title_Code_No as double),cast(Level as int),cast(FullTime_PartTime as int),cast(Salary_Range_To as double),cast(Salary_Frequency as int),cast(Hours_Shift as int) FROM nycjobs\")"],"metadata":{},"outputs":[],"execution_count":3},{"cell_type":"code","source":["jobs.show(5)"],"metadata":{},"outputs":[],"execution_count":4},{"cell_type":"code","source":["jobs1=jobs.dropna()"],"metadata":{},"outputs":[],"execution_count":5},{"cell_type":"code","source":["data = jobs1.select('Job_ID','Posting_Type','Title_Code_No','Level','FullTime_PartTime',col('Salary_Range_To').alias('label'),'Salary_Frequency','Hours_Shift')"],"metadata":{},"outputs":[],"execution_count":6},{"cell_type":"code","source":["data.show(5)"],"metadata":{},"outputs":[],"execution_count":7},{"cell_type":"markdown","source":["#### Split the Data"],"metadata":{}},{"cell_type":"code","source":["splits = data.randomSplit([0.7, 0.3])\ntrain = splits[0]\ntest = splits[1].withColumnRenamed(\"label\", \"trueLabel\")"],"metadata":{},"outputs":[],"execution_count":9},{"cell_type":"markdown","source":["###Gradient Booster Tree Regression"],"metadata":{}},{"cell_type":"code","source":["assembler = VectorAssembler(inputCols = ['Job_ID','Posting_Type','Title_Code_No','Level','FullTime_PartTime','Salary_Frequency','Hours_Shift'], outputCol=\"features\")\ngbt = GBTRegressor(labelCol=\"label\")"],"metadata":{},"outputs":[],"execution_count":11},{"cell_type":"markdown","source":["####Tune Parameters"],"metadata":{}},{"cell_type":"code","source":["paramGrid = ParamGridBuilder()\\\n  .addGrid(gbt.maxDepth, [2, 5])\\\n  .addGrid(gbt.maxIter, [10, 100])\\\n  .build()\n  \nevaluator = RegressionEvaluator(metricName=\"rmse\", labelCol=gbt.getLabelCol(), predictionCol=gbt.getPredictionCol())\n\ncv = CrossValidator(estimator=gbt, evaluator=evaluator, estimatorParamMaps=paramGrid)"],"metadata":{},"outputs":[],"execution_count":13},{"cell_type":"markdown","source":["####Define the Pipeline"],"metadata":{}},{"cell_type":"code","source":["pipeline = Pipeline(stages=[assembler, cv])\npipelineModel = pipeline.fit(train)"],"metadata":{},"outputs":[],"execution_count":15},{"cell_type":"markdown","source":["####Test the Model"],"metadata":{}},{"cell_type":"code","source":["predictions = pipelineModel.transform(test)"],"metadata":{},"outputs":[],"execution_count":17},{"cell_type":"code","source":["predicted = predictions.select(\"features\", \"prediction\", \"trueLabel\")\npredicted.show(100)"],"metadata":{},"outputs":[],"execution_count":18},{"cell_type":"markdown","source":["####Examine the Predicted and Actual Values"],"metadata":{}},{"cell_type":"code","source":["predicted.createOrReplaceTempView(\"regressionPredictions\")\n"],"metadata":{},"outputs":[],"execution_count":20},{"cell_type":"code","source":["dataPred = spark.sql(\"SELECT trueLabel, prediction FROM regressionPredictions\")\n\ndisplay(dataPred)"],"metadata":{},"outputs":[],"execution_count":21},{"cell_type":"markdown","source":["####RMSE Analysis"],"metadata":{}},{"cell_type":"code","source":["evaluator  = RegressionEvaluator(labelCol=\"trueLabel\", predictionCol=\"prediction\", metricName=\"rmse\")\nrmse = evaluator.evaluate(predictions)\nprint \"Root Mean Square Error (RMSE) for GBT Regression :\", rmse"],"metadata":{},"outputs":[],"execution_count":23},{"cell_type":"markdown","source":["###Linear Regression"],"metadata":{}},{"cell_type":"code","source":["assembler = VectorAssembler(inputCols = ['Job_ID','Posting_Type','Title_Code_No','Level','FullTime_PartTime','Salary_Frequency','Hours_Shift'], outputCol=\"features\")\nlr = LinearRegression(labelCol=\"label\",featuresCol=\"features\", maxIter=10, regParam=0.3)\npipeline1 = Pipeline(stages=[assembler, lr])\n"],"metadata":{},"outputs":[],"execution_count":25},{"cell_type":"markdown","source":["####Tune Parameters"],"metadata":{}},{"cell_type":"code","source":["paramGrid1 = ParamGridBuilder().addGrid(lr.regParam, [0.3, 0.01]).addGrid(lr.maxIter, [10, 5]).build()\ntrainval = TrainValidationSplit(estimator=pipeline1, evaluator=RegressionEvaluator(), estimatorParamMaps=paramGrid1, trainRatio=0.8)"],"metadata":{},"outputs":[],"execution_count":27},{"cell_type":"markdown","source":["####Define the Pipeline"],"metadata":{}},{"cell_type":"code","source":["pipelineModel = trainval.fit(train)"],"metadata":{},"outputs":[],"execution_count":29},{"cell_type":"markdown","source":["####Test the Model"],"metadata":{}},{"cell_type":"code","source":["predictions = pipelineModel.transform(test)"],"metadata":{},"outputs":[],"execution_count":31},{"cell_type":"code","source":["predicted = predictions.select(\"features\", \"prediction\", \"trueLabel\")\npredicted.show(100)"],"metadata":{},"outputs":[],"execution_count":32},{"cell_type":"markdown","source":["####Examine the Predicted and Actual Values"],"metadata":{}},{"cell_type":"code","source":["predicted.createOrReplaceTempView(\"regressionPredictions\")\n"],"metadata":{},"outputs":[],"execution_count":34},{"cell_type":"code","source":["dataPred = spark.sql(\"SELECT trueLabel, prediction FROM regressionPredictions\")\n\ndisplay(dataPred)"],"metadata":{},"outputs":[],"execution_count":35},{"cell_type":"markdown","source":["####RMSE Analysis"],"metadata":{}},{"cell_type":"code","source":["evaluator  = RegressionEvaluator(labelCol=\"trueLabel\", predictionCol=\"prediction\", metricName=\"rmse\")\nrmse = evaluator.evaluate(predictions)\nprint \"Root Mean Square Error (RMSE) for Linear Regression :\", rmse"],"metadata":{},"outputs":[],"execution_count":37},{"cell_type":"code","source":[""],"metadata":{},"outputs":[],"execution_count":38}],"metadata":{"name":"nycjobs","notebookId":573868716777732},"nbformat":4,"nbformat_minor":0}
